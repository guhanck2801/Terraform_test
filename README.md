This repository contains the file and folders from my first project experimenting with terraform. In this project, I used terraform to access AWS servers and configure and s3 bucket, as well as convert a local python file into a lambda function that would move a file from one folder to another when dropped into the 
s3 bucket.
